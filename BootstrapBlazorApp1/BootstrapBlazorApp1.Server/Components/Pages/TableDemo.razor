@page "/table"
@attribute [TabItemOption(Text = "Table")]

<PageTitle>Table</PageTitle>

<Table TItem="Foo" AutoGenerateColumns="true" class="table-demo"
       IsPagination="true" PageItemsSource="PageItemsSource"
       IsStriped="true" IsBordered="true"IsMultipleSelect="true"
       ShowSkeleton="true" ShowToolbar="true" ShowSearch="true" ShowExtendButtons="true" ShowExportButton="true" ShowExportPdfButton="true"
       OnQueryAsync="OnQueryAsync" OnSaveAsync="OnSaveAsync" OnDeleteAsync="OnDeleteAsync" ShowExportCsvButton="true">
    <TableColumns>
        <TableColumn @bind-Field="@context.Id" Text="Action">
            <Template Context="v">
                <Button class="btn btn-primary btn-xs" Text="View" Size="Size.Small" OnClick="@(() => ApplyFilenameStream())" />
            </Template>
            </TableColumn>
        <TableColumn @bind-Field="@context.Hobby" Items="GetHobbys(context)" />
    </TableColumns>
</Table>
<br />
<FileViewer @ref="fileViewer" Filename="@FilenameStream" />
